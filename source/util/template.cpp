#include <iostream>   // standard input output
#include <algorithm> // sort 
#include <cstring>

using namespace std;

int main(void) {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int T = 0;
    cin >> T;

}